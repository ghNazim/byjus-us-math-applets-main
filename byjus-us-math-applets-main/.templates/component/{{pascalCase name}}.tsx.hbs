import { FC } from 'react'

import { {{pascalCase name}}Props } from './{{pascalCase name}}.types'

export const {{pascalCase name}}: FC<{{pascalCase name}}Props> = ({ foo }) => {
  return <div data-testid='{{kebabCase name}}'>{foo}</div>
}
